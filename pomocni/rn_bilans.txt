DROP TABLE `rn_bilans`;

CREATE TABLE `rn_bilans` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `rn_id` bigint(20),
  `datumr` datetime DEFAULT NULL,
  `brojilo_id` bigint(20) DEFAULT NULL,
  `dobavljac_id` bigint(20) DEFAULT NULL,
  `energent_id` bigint(20) DEFAULT NULL,
  `rn_tip_id` bigint(20) DEFAULT NULL,
  `iznos` decimal(18,2) DEFAULT NULL,
  `kolicina` decimal(18,2) DEFAULT NULL,
  `kolicina_kwh` decimal(21,5) DEFAULT NULL,
  `kolicina_kwh_pov` decimal(21,5) DEFAULT NULL,
  `kolicina_kwh_zap` decimal(21,5) DEFAULT NULL,
  `kolicina_kwh_kor` decimal(21,5) DEFAULT NULL,
  `kolicina_fin_ene` decimal(21,5) DEFAULT NULL,
  `kolicina_prim_ene` decimal(21,5) DEFAULT NULL,
  `emisija_co2` decimal(21,5) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `RN_BILANS_FK1` (`brojilo_id`),
  KEY `RN_BILANS_FK2` (`energent_id`),
  KEY `RN_BILANS_FK3` (`dobavljac_id`),
  KEY `RN_BILANS_FK4` (`rn_tip_id`),
  KEY `RN_BILANS_FK5` (`rn_id`),
  CONSTRAINT `RN_BILANS_FK1` FOREIGN KEY (`brojilo_id`) REFERENCES `brojilo` (`id`),
  CONSTRAINT `RN_BILANS_FK2` FOREIGN KEY (`energent_id`) REFERENCES `energent` (`id`),
  CONSTRAINT `RN_BILANS_FK3` FOREIGN KEY (`dobavljac_id`) REFERENCES `dobavljac` (`id`),
  CONSTRAINT `RN_BILANS_FK4` FOREIGN KEY (`rn_tip_id`) REFERENCES `rn_tip` (`id`),
  CONSTRAINT `RN_BILANS_FK5` FOREIGN KEY (`rn_id`) REFERENCES `rn` (`id`) ON DELETE CASCADE
);



insert into rn_bilans(`id`,
  `datumr`,
  `brojilo_id`,
  `dobavljac_id`,
  `energent_id`, 
  `rn_tip_id`,
  `iznos`,
  `kolicina`,
  `kolicina_kwh`,
  `kolicina_kwh_pov`,
  `kolicina_kwh_zap`,
  `kolicina_kwh_kor`,
  `kolicina_fin_ene`,
  `kolicina_prim_ene`,
  `emisija_co2`)
select `id`,
  `datumr`,
  `brojilo_id`,
  `dobavljac_id`,
  `energent_id`, 
  `rn_tip_id`,
  `iznos`,
  `kolicina`,
  `kolicina_kwh`,
  `kolicina_kwh_pov`,
  `kolicina_kwh_zap`,
  `kolicina_kwh_kor`,
  `kolicina_fin_ene`,
  `kolicina_prim_ene`,
  `emisija_co2`
from rn;


CREATE DEFINER=`isem`@`%` TRIGGER `heroku_75858eca71cd74b`.`rn_AFTER_INSERT` AFTER INSERT ON `rn` FOR EACH ROW
BEGIN
	DECLARE p_kwh_jm, p_emisija, p_primarna_ene, p_finalna_ene DOUBLE;
      DECLARE p_grej_po_korisna, p_grej_za, p_op_br_kor DOUBLE;
      DECLARE p_koef DOUBLE;
      DECLARE p_id BIGINT(20);   
      DECLARE done BOOLEAN;
      
      DECLARE cur CURSOR FOR SELECT id FROM brojilo_koef_v WHERE vodece_brojilo_id = NEW.brojilo_id;
	  DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;
      
      -- koeficijenti za energent --
      select kwh_jm, emisija, primarna_energija, finalna_energija
      into p_kwh_jm, p_emisija, p_primarna_ene, p_finalna_ene
      from energent
      where id = NEW.`energent_id`;
      
      
      -- glavno brojilo --           
      select grej_po_korisna, grej_za, op_br_kor
      into p_grej_po_korisna, p_grej_za, p_op_br_kor
      from objekat o, brojilo b
      where o.id = b.objekat_id
			and b.id = NEW.`brojilo_id`;
            
	  select koef into p_koef
      from brojilo_koef_v
      where id = NEW.`brojilo_id`;
	
      INSERT INTO rn_bilans(`rn_id`,
						  `datumr`,
						  `brojilo_id`,
						  `dobavljac_id`,
						  `energent_id`, 
						  `rn_tip_id`,
						  `iznos`,
                          `iznos_pov`,
                          `iznos_zap`,
                          `iznos_kor`,
						  `kolicina`,
                          `kolicina_pov`,
						  `kolicina_zap`,
						  `kolicina_kor`,
						  `kolicina_kwh`,
						  `kolicina_kwh_pov`,
						  `kolicina_kwh_zap`,
						  `kolicina_kwh_kor`,
						  `kolicina_fin_ene`,
						  `kolicina_prim_ene`,
						  `emisija_co2`,
                          `emisija_co2_pov`,
                          `emisija_co2_zap`,
                          `emisija_co2_kor`)
		VALUES(NEW.`id`,
			  NEW.`datumr`,
			  NEW.`brojilo_id`,
			  NEW.`dobavljac_id`,
			  NEW.`energent_id`, 
			  NEW.`rn_tip_id`,
			  NEW.`iznos` * p_koef,
              NEW.`iznos` / p_grej_po_korisna * p_koef,
              NEW.`iznos` / p_grej_za * p_koef,
              NEW.`iznos` / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_koef,
              NEW.`kolicina` / p_grej_po_korisna * p_koef,
              NEW.`kolicina` / p_grej_za * p_koef,
              NEW.`kolicina` / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_kwh_jm * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_grej_po_korisna * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_grej_za * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_finalna_ene * p_koef,
			  NEW.`kolicina` * p_primarna_ene * p_koef,
			  NEW.`kolicina` * p_emisija * p_koef,
              NEW.`kolicina` * p_emisija / p_grej_po_korisna * p_koef,
              NEW.`kolicina` * p_emisija / p_grej_za * p_koef,
              NEW.`kolicina` * p_emisija / p_op_br_kor * p_koef);
              
		-- virtuelna brojila --        
        OPEN cur;
        ins_loop: LOOP
            FETCH cur INTO p_id;
            IF done THEN
                LEAVE ins_loop;
            END IF;
            
            select grej_po_korisna, grej_za, op_br_kor
		    into p_grej_po_korisna, p_grej_za, p_op_br_kor
		    from objekat o, brojilo b
		    where o.id = b.objekat_id
				 and b.id = p_id;
				
		    select koef into p_koef
		    from brojilo_koef_v
		    where id = p_id;
            
            INSERT INTO rn_bilans(`rn_id`,
						  `datumr`,
						  `brojilo_id`,
						  `dobavljac_id`,
						  `energent_id`, 
						  `rn_tip_id`,
						  `iznos`,
                          `iznos_pov`,
                          `iznos_zap`,
                          `iznos_kor`,
						  `kolicina`,
                          `kolicina_pov`,
						  `kolicina_zap`,
						  `kolicina_kor`,
						  `kolicina_kwh`,
						  `kolicina_kwh_pov`,
						  `kolicina_kwh_zap`,
						  `kolicina_kwh_kor`,
						  `kolicina_fin_ene`,
						  `kolicina_prim_ene`,
						  `emisija_co2`,
                          `emisija_co2_pov`,
                          `emisija_co2_zap`,
                          `emisija_co2_kor`)
		VALUES(NEW.`id`,
			  NEW.`datumr`,
			  NEW.`brojilo_id`,
			  NEW.`dobavljac_id`,
			  NEW.`energent_id`, 
			  NEW.`rn_tip_id`,
			  NEW.`iznos` * p_koef,
              NEW.`iznos` / p_grej_po_korisna * p_koef,
              NEW.`iznos` / p_grej_za * p_koef,
              NEW.`iznos` / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_koef,
              NEW.`kolicina` / p_grej_po_korisna * p_koef,
              NEW.`kolicina` / p_grej_za * p_koef,
              NEW.`kolicina` / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_kwh_jm * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_grej_po_korisna * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_grej_za * p_koef,
			  NEW.`kolicina` * p_kwh_jm / p_op_br_kor * p_koef,
			  NEW.`kolicina` * p_finalna_ene * p_koef,
			  NEW.`kolicina` * p_primarna_ene * p_koef,
			  NEW.`kolicina` * p_emisija * p_koef,
              NEW.`kolicina` * p_emisija / p_grej_po_korisna * p_koef,
              NEW.`kolicina` * p_emisija / p_grej_za * p_koef,
              NEW.`kolicina` * p_emisija / p_op_br_kor * p_koef);            
            
        END LOOP;
		CLOSE cur;
END


CREATE DEFINER=`isem`@`%` TRIGGER `heroku_75858eca71cd74b`.`rn_AFTER_UPDATE` AFTER UPDATE ON `rn` FOR EACH ROW
BEGIN
	DECLARE p_kwh_jm, p_emisija, p_primarna_ene, p_finalna_ene DOUBLE;
      DECLARE p_grej_po_korisna, p_grej_za, p_op_br_kor DOUBLE;
      DECLARE p_koef DOUBLE;
      DECLARE p_id BIGINT(20);   
      DECLARE done BOOLEAN;
      
      DECLARE cur CURSOR FOR SELECT id FROM brojilo_koef_v WHERE vodece_brojilo_id = NEW.brojilo_id;
	  DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;
      
      -- koeficijenti za energent --
      select kwh_jm, emisija, primarna_energija, finalna_energija
      into p_kwh_jm, p_emisija, p_primarna_ene, p_finalna_ene
      from energent
      where id = NEW.`energent_id`;
      
      
      -- glavno brojilo --           
      select grej_po_korisna, grej_za, op_br_kor
      into p_grej_po_korisna, p_grej_za, p_op_br_kor
      from objekat o, brojilo b
      where o.id = b.objekat_id
			and b.id = NEW.`brojilo_id`;
            
	  select koef into p_koef
      from brojilo_koef_v
      where id = NEW.`brojilo_id`;           
        
      UPDATE rn_bilans 
      SET `rn_id` = NEW.`id`,
		  `datumr` = NEW.`datumr`,
		  `brojilo_id` = NEW.`brojilo_id`,
		  `dobavljac_id` = NEW.`dobavljac_id`,
		  `energent_id` = NEW.`energent_id`, 
		  `rn_tip_id` = NEW.`rn_tip_id`,
		  `iznos` = NEW.`iznos` * p_koef,
          `iznos_pov` = NEW.`iznos` / p_grej_po_korisna * p_koef,
		  `iznos_zap` = NEW.`iznos` / p_grej_za * p_koef,
		  `iznos_kor` = NEW.`iznos` / p_op_br_kor * p_koef,
		  `kolicina` = NEW.`kolicina` * p_koef,
          `kolicina_pov` = NEW.`kolicina` / p_grej_po_korisna * p_koef, 
		  `kolicina_zap` = NEW.`kolicina` / p_grej_za * p_koef,
		  `kolicina_kor` = NEW.`kolicina` / p_op_br_kor * p_koef,
		  `kolicina_kwh` = NEW.`kolicina` * p_kwh_jm * p_koef,
		  `kolicina_kwh_pov` = NEW.`kolicina` * p_kwh_jm / p_grej_po_korisna * p_koef,
		  `kolicina_kwh_zap` = NEW.`kolicina` * p_kwh_jm / p_grej_za * p_koef,
		  `kolicina_kwh_kor` = NEW.`kolicina` * p_kwh_jm / p_op_br_kor * p_koef,
		  `kolicina_fin_ene` = NEW.`kolicina` * p_finalna_ene * p_koef,
		  `kolicina_prim_ene` = NEW.`kolicina` * p_primarna_ene * p_koef,
		  `emisija_co2` = NEW.`kolicina` * p_emisija * p_koef,
          `emisija_co2_pov` = NEW.`kolicina` * p_emisija / p_grej_po_korisna * p_koef,
		  `emisija_co2_zap` = NEW.`kolicina` * p_emisija / p_grej_za * p_koef,
		  `emisija_co2_kor` = NEW.`kolicina` * p_emisija / p_op_br_kor * p_koef
      WHERE rn_id = NEW.id and brojilo_id = NEW.`brojilo_id`;
      
      -- virtuelna brojila --        
        OPEN cur;
        ins_loop: LOOP
            FETCH cur INTO p_id;
            IF done THEN
                LEAVE ins_loop;
            END IF;
            
            select grej_po_korisna, grej_za, op_br_kor
		    into p_grej_po_korisna, p_grej_za, p_op_br_kor
		    from objekat o, brojilo b
		    where o.id = b.objekat_id
				 and b.id = p_id;
				
		    select koef into p_koef
		    from brojilo_koef_v
		    where id = p_id;            

                  
			UPDATE rn_bilans 
			  SET `rn_id` = NEW.`id`,
				  `datumr` = NEW.`datumr`,
				  `brojilo_id` = NEW.`brojilo_id`,
				  `dobavljac_id` = NEW.`dobavljac_id`,
				  `energent_id` = NEW.`energent_id`, 
				  `rn_tip_id` = NEW.`rn_tip_id`,
				  `iznos` = NEW.`iznos` * p_koef,
				  `iznos_pov` = NEW.`iznos` / p_grej_po_korisna * p_koef,
				  `iznos_zap` = NEW.`iznos` / p_grej_za * p_koef,
				  `iznos_kor` = NEW.`iznos` / p_op_br_kor * p_koef,
				  `kolicina` = NEW.`kolicina` * p_koef,
				  `kolicina_pov` = NEW.`kolicina` / p_grej_po_korisna * p_koef, 
				  `kolicina_zap` = NEW.`kolicina` / p_grej_za * p_koef,
				  `kolicina_kor` = NEW.`kolicina` / p_op_br_kor * p_koef,
				  `kolicina_kwh` = NEW.`kolicina` * p_kwh_jm * p_koef,
				  `kolicina_kwh_pov` = NEW.`kolicina` * p_kwh_jm / p_grej_po_korisna * p_koef,
				  `kolicina_kwh_zap` = NEW.`kolicina` * p_kwh_jm / p_grej_za * p_koef,
				  `kolicina_kwh_kor` = NEW.`kolicina` * p_kwh_jm / p_op_br_kor * p_koef,
				  `kolicina_fin_ene` = NEW.`kolicina` * p_finalna_ene * p_koef,
				  `kolicina_prim_ene` = NEW.`kolicina` * p_primarna_ene * p_koef,
				  `emisija_co2` = NEW.`kolicina` * p_emisija * p_koef,
				  `emisija_co2_pov` = NEW.`kolicina` * p_emisija / p_grej_po_korisna * p_koef,
				  `emisija_co2_zap` = NEW.`kolicina` * p_emisija / p_grej_za * p_koef,
				  `emisija_co2_kor` = NEW.`kolicina` * p_emisija / p_op_br_kor * p_koef
			  WHERE rn_id = NEW.id and brojilo_id = NEW.`brojilo_id`;
            
        END LOOP;
		CLOSE cur;
END